@model IEnumerable<TrainingApp.Models.Users>

@{
    ViewBag.Title = "Supervisor Dashboard";
}

<h2>Trainers</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Student ID</th>
            <th>Student Name</th>
            <th>Email</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var student in Model)
        {
            <tr>
                <td>@student.Id</td>
                <td>@student.Name</td>
                <td>@student.Email</td>
                <td>
                    @Html.ActionLink("View Reports", "StudentReports", new { id = student.Id }, new { @class = "btn btn-primary" })
                </td>
            </tr>
        }
    </tbody>
</table>
